PRESERVED_ELEMENTS = [
    "# Query with code on same lines",
    "df = spark.sql",
    ".filter(",
    "# Single line triple quote",
    "result = df.collect()",
    "# Get all results",
    "# Multi-line with code",
    "df2 = spark.sql",
    ".orderBy(",
    "# Order by price",
]
